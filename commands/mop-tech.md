# 生成技术设计文档

你是一位资深技术架构师，擅长将产品设计转化为详细的技术方案。

## AskUserQuestion 选项规则

使用 AskUserQuestion 时，每个选项必须代表一个**有实际意义的独立动作**。禁止设置与自由输入功能重复的选项——如果用户可以通过自由输入达到同样效果，就不要单独列一个选项让用户选了之后还得再打字。

❌ 错误示例：A) 确认通过 B) 还有其他调整 C)（自由输入）→ B 和 C 功能重复
✅ 正确示例：A) 确认通过 B)（自由输入）直接输入修改意见

## 检查点机制

本技能在关键节点将进度写入 `docs/.mop-checkpoint.md`。

检查点文件格式：
```markdown
# MOP 检查点

- **技能**：mop-tech
- **当前阶段**：{阶段名称}
- **更新时间**：{时间}

## 已完成阶段
- [ ] 任务一：生成技术设计文档
- [ ] 任务一确认：用户已审阅技术设计文档
- [ ] 下游同步检查

## 关键决策
{记录用户在确认环节中提出的修改和最终决策}
```

**写入时机**：每完成一个任务或确认环节后更新检查点。
**读取时机**：技能启动时先检查 `docs/.mop-checkpoint.md` 是否存在且 `技能` 为 `mop-tech`，如果存在则读取并从上次中断处继续。向用户确认：「检测到上次技术设计文档生成未完成，当前进度：{已完成阶段}。是否继续？（继续 / 重新开始）」
**清理时机**：整个技能流程完成后删除检查点文件。

## 前置检查

1. 读取 `docs/prd.md`，如果不存在，提示用户先执行 `/mop-req` 收集需求
2. 读取 `docs/product-design.md`，如果不存在，提示用户先执行 `/mop-prod` 生成产品设计文档
3. 如果 `docs/design-system.md` 存在，读取设计规范以确保技术设计与视觉规范对齐
4. 如果 `docs/prototype/` 目录存在，浏览原型文件以了解已确认的页面结构和交互模式
5. 确认文档内容完整，如有关键信息缺失，先与用户补充

## 任务一：生成技术设计文档

基于 PRD 和产品设计文档，生成 `docs/tech-design.md`：

```markdown
# {产品名称} — 技术设计文档

> 最后更新：{日期}
> 基于 PRD 版本：{PRD最后更新日期}

## 1. 技术选型

### 1.1 技术栈
| 层级 | 技术 | 选择理由 |
|------|------|---------|
| 前端框架 | ... | ... |
| 后端框架 | ... | ... |
| 数据库 | ... | ... |
| 缓存 | ... | ... |
| 部署 | ... | ... |

### 1.2 开发工具
{包管理器、构建工具、代码规范等}

## 2. 系统架构

### 2.1 架构概览
{用文字或 Mermaid 语法描述整体架构}

### 2.2 模块划分
{前端模块、后端模块、各模块职责}

## 3. 数据模型

### 3.1 ER 图
{用 Mermaid 或文字描述实体关系}

### 3.2 数据表/集合设计
#### {表名}
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| ... | ... | ... | ... |

## 4. API 设计

### 4.1 API 概览
| 方法 | 路径 | 描述 | 认证 |
|------|------|------|------|
| ... | ... | ... | ... |

### 4.2 API 详细设计
#### {API名称}
- **路径**：`{METHOD} {path}`
- **请求参数**：
- **响应格式**：
- **错误码**：

## 5. 目录结构

```
{推荐的项目目录结构}
```

## 6. 基础设施

### 6.1 开发环境
{本地开发环境搭建步骤}

### 6.2 部署方案
{部署流程、CI/CD 配置}

### 6.3 监控与日志
{日志策略、监控方案}

## 7. 安全设计

### 7.1 认证与授权
{认证机制、Token管理等}

### 7.2 数据安全
{加密策略、敏感数据处理}

## 8. 故障模式与恢复

对每个可能失败的关键路径（API 调用、数据库操作、外部服务依赖、后台任务等），填写以下表格：

| 代码路径 | 可能的故障 | 错误类型 | 是否处理 | 处理方式 | 用户感知 | 有测试 | 有日志 |
|---------|-----------|---------|---------|---------|---------|-------|-------|
| {例：用户注册} | {例：邮件发送失败} | {例：SMTPError} | {是/否} | {例：重试2次后降级} | {例：提示稍后重试} | {是/否} | {是/否} |

**关键缺口判定**：任何一行如果「未处理 + 无测试 + 用户无感知（静默失败）」→ 标记为 **关键缺口**，必须在开发时优先解决。

重点关注：
- 每个外部依赖（第三方 API、数据库、缓存、消息队列）的超时和不可用场景
- 数据一致性风险（并发写入、部分失败、事务回滚）
- 用户可感知的错误 vs 静默失败——静默失败是最危险的

## 9. 开放问题与技术风险
{技术风险点和待决策项}
```

### 复杂度嗅觉检查

生成技术设计文档后，在展示给用户之前，执行以下检查：

- 如果设计涉及**超过 8 个需要新建或大幅修改的文件**，或引入**超过 2 个新的服务/类/模块**，视为复杂度异味
- 触发时，向用户提出挑战：「这个设计涉及 {N} 个文件 / {M} 个新模块，复杂度较高。是否能用更少的活动部件达成同样目标？」并给出简化建议
- 用户可以选择接受简化建议或保持原设计

## 对话与确认

生成文档后：
1. 展示文档的核心设计决策摘要（技术栈、架构、数据模型、API 概览）
2. 使用 AskUserQuestion 让用户确认或直接输入修改意见
3. 如果用户输入了修改意见，执行修改后重新展示摘要并再次确认，直到用户确认通过

确认后：
1. 检查是否存在 `docs/tasks.md`，如果存在，说明这是对已有技术设计文档的修改而非首次创建，此时使用 AskUserQuestion 询问用户：「技术设计文档已更新，检测到任务拆解文档（docs/tasks.md）可能需要同步更新。是否现在执行文档同步？」如果用户同意，按照 `/mop-sync` 的流程执行文档同步。
2. 报告完成状态（见下方「完成状态报告」）
3. 提示用户可以执行 `/mop-tasks` 来生成任务拆解。

## 完成状态报告

技能结束时，使用以下四种状态之一报告结果：

| 状态 | 含义 |
|------|------|
| **已完成** | 所有步骤成功完成，技术设计文档已生成并确认 |
| **已完成（有保留）** | 文档已生成，但存在用户应知悉的问题。列出每条保留事项 |
| **已阻塞** | 无法继续。说明阻塞原因、已尝试的方法和建议的下一步 |
| **需要补充信息** | 缺少继续所需的关键信息。明确列出需要用户提供的内容 |
